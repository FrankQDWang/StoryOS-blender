# StoryOS Library — 已确认方案与执行记录

更新日期：2026-09-16（Asia/Singapore）。

## 当前交接

- **2026-09-16：用户实际查看后确认“可以，这个我比较满意”，并明确要求提交、push，保存当前进度。** 当前认可版本为 `0.1.2-preview.4`，后排 0°、中段内收 10°、前景内收 20°。本次保存范围包括已完成的暖炉氛围、统一布局/漫游碰撞、局部角度与点击修复、Blender/GLB/纹理、参考来源和各轮验收证据；沿用本轮已通过的测试、构建、几何及 Chrome 结果。
- **保存方式**：在 `iteration/next` 新增进度提交并推送同名远端分支；不合入 `main`、不新增或移动标签。浏览器本地作品和设置仍保留在原 origin，不属于 Git 快照。以下为制作和验证历史，旧“未提交”“待评价”状态由本次认可与保存授权覆盖。
- **2026-09-16：已完成用户批准的后排 0°、中段内收 10°、前景内收 20° 局部方案，当前预览 `0.1.2-preview.4`。** 五台位置和阅读斜面沿用；后排两台摆正，左前 +20°、右中 -10°、右前 -20°。统一布局驱动重导出的台面/支架、书本、镜头和碰撞。本条覆盖下方暂停状态；角度方案已获实施授权，实际审美效果仍待用户评价。
- **本轮已验证**：Chrome 同视口前后全景、后排直接选中与近景切漫游、W 接近后 E 选书、完整开书进入《守月人》并返回、前景 20° 书本选中与近景均通过。10 项既有测试、Blender 保存几何与最终构建通过；五台站位/聚焦/开书落点可走，本组角度无需镜头水平安全修正；圆台闭环保持。Book/Hands 源顶点与 baseline 一致，生产 GLB 与本次导出一致，Chrome 错误日志为空。
- **本轮交付**：证据 `evidence/v012-gentle-angles/`，`01-before.jpg` / `02-after.jpg` / `07-overview-final.jpg` 均为 1512×751 实际 Chrome 画面。预览 `http://127.0.0.1:4173/` 已恢复全景并保留 3 本示例；未新增素材，沿用 `design/round-04/01-open-hearth.png`。既有光点拾取修复、手部动作及指针锁定降级限制保留。继续在 `iteration/next` 未提交工作区，无推送或分支/标签移动。
- **2026-09-16：用户明确否定 `0.1.2-preview.3` 的朝向效果，要求先讨论新方案，暂停场景改动。** 用户希望最里面两本书不作水平旋转，其余两层的角度变化更平滑。当前讨论候选为后排两台 yaw 0°、右侧中台内收约 10°、前景左右两台内收约 20°；五台位置及原有阅读斜面先沿用。这是待讨论方案，尚未实施或验证，不能把此前 10° → 21° → 30° 的技术检查通过当作用户审美认可；光点误拦截的独立修复仍保留。
- **2026-09-16：已完成用户授权的后排两台内收，当前预览 `0.1.2-preview.3`，待用户看实际效果。** 后左 yaw +0.18、后右 -0.18 rad（各内收约 10.3°），其余三个角度及所有位置沿用。右侧从后到前内收约 10° → 21° → 30°，消除后排与中段的反折。模型、书本、支架、镜头、碰撞继续从统一布局读取。手部源造型和动作、整体材质与光照保持。
- **本轮检查与联动修复**：Chrome 后排两书直接选中、左后聚焦转漫游与 W/E 接近选书、右后完整开书进入工作区并返回通过。点选时确认中央台 Sparkles 的 Points 拾取半径会拦截后排书，故仅禁用该装饰粒子的 raycast；同坐标复测正确选书，圆台实体创建入口仍有效，取消未新增项目。10 项测试、Blender 实际几何、五台站位/聚焦/开书落点及最终构建通过；生产 GLB 与源导出一致，Book/Hands 顶点与 baseline 一致，Chrome 错误日志为空。
- **本轮证据和交付**：`evidence/v012-rear-inward/` 保存前后全景、后排近景/开书/漫游、粒子拦截诊断和检查结果；`07-overview-final.jpg` 为最终实际画面。`http://127.0.0.1:4173/` 已打开全景并保持 3 本示例。无新增图像素材，沿用 `design/round-04/01-open-hearth.png`；继续保留在 `iteration/next` 未提交工作区，未推送或移动任何分支/标签。既有手部接触细节及指针锁定降级限制沿用。
- **2026-09-16：用户反馈后排两台与中段衔接不平滑，当前只讨论后续修正。** 现状右侧从后向前为外撇约 9° → 内收约 21° → 内收约 30°，后排与中段存在转向反折；左侧也有后排向外、前排向内的方向变化。助手建议后排两台也轻微内收约 8–12°，保持接近正面的迎入口姿态，让由后到前的内收逐渐增强。具体角度尚未实施或验证，五台位置先沿用当前布局；这条用户反馈优先于上一轮局部视觉验收的完成表述。
- **2026-09-16：已完成用户授权的阅读台局部转向，资产版本 `0.1.2-preview.2`，待用户体验评价。** 五台位置保持；左前 yaw 改为 +0.48、右中 -0.36、右前 -0.52 rad，后排两台沿用 -0.15/+0.15。台面、书本、支架与碰撞采用同一朝向，聚焦/开书镜头也随之转向。阅读镜头下方若无站位，从台前可达点按真实碰撞求得安全落点；本轮只有右后台因此水平微调约 15 cm，避免从近景进入漫游时返回入口。
- **本次验证**：Chrome 同视口前后截图 `evidence/v012-orientation/01-before.jpg` / `02-after.jpg`；炉口可见、中央空地保持。左右前台聚焦及开书到正确工作区、新书落到右前台、右后台近景原地进入漫游、W 前进后 E 选书均通过。10 项既有测试、保存后的 Blender 几何检查和最终生产构建通过；五台聚焦/开书终点投影可走，生产 GLB 与源导出逐字节相同，Chrome 错误日志为空。几何证明 Book/Hands 世界顶点与 baseline 不变；手部 GLB 重导出字节有变化，未修改其源造型或动作。指针锁定仍走拖拽降级，不宣称原生锁定验证。
- **本次交付**：预览 `http://127.0.0.1:4173/` 保留 3 本示例；新增测试书只在独立 localhost origin。新证据集中于 `evidence/v012-orientation/`，此前验收保留。沿用原资产和选定参考，无新增图像素材。继续留在 `iteration/next` 未提交工作区，未推送、发布或移动分支/标签；现有手部接触细节限制仍暂缓。
- **2026-09-16：讨论五个阅读台的水平朝向，未调整场景。** 用户询问全部向外是否符合美学。只读核对当前布局与旋转映射：五台主要朝入口，左侧向左外撇、右侧向右外撇；这与桌面的阅读倾角是两回事。结合已有实际预览截图，助手建议后排两台保留迎向入口的关系、侧边与前景台适度转向中央通道，按各自接近路线形成松散围合；这是待用户讨论的建议，尚未采纳具体角度，也未制作或验证新布局。
- **2026-09-16：完成 Blender CLI / MCP 工具调研。** 记录见 `docs/research/2026-09-16-blender-cli-mcp.md`。本机实查 Blender CLI 为 5.2.1 LTS，现有 `package.json` 的资产生成已使用官方后台命令；社区 MCP 与第三方 CLI 仅核对上游文档及仓库信息，未安装或运行验证。本轮未修改场景、资产或交互，未变更分支与标签。建议继续保留官方 CLI 制作链，需要交互式场景控制时再评估 MCP。
- **2026-09-14：已完成本轮真实三维预览，资产版本 `0.1.2-preview.1`，待用户体验评价。** 入口仍为 `http://127.0.0.1:4173/`。所选 `design/round-04/01-open-hearth.png` 是视觉目标；实际结果为 `evidence/v012/overview.png`，两者同框对照在 `evidence/v012/comparison-final.png`。概念图与实现截图分别保存，不宣称逐像素或离线渲染级还原。
- **已实现**：以恢复版继续改善炉火、炉台烛光与地板暖反光；强化木纹/灰泥表面，方形弯曲木梁、完整烟囱、夜窗树影、少量挂毯/画框/炉台陈设。左前台移向墙边，解除书本遮住炉口的问题，保持五个独立展示位及中央空地。补齐相机背后的墙与关闭的装饰门，没有新增房间或业务入口。
- **空间实现与验证**：`apps/web/src/room-layout.json` 统一书位、桌台中心及导航范围；修正 Blender Z 旋转到 Three Y 旋转的符号、书本 pitch/yaw 顺序。半径 0.25 m 的身体与旋转家具碰撞并可沿表面滑动；书桌把手/椅背纳入碰撞。10/10 测试通过，实际移动函数证明五台正面可达、圆台半径 1.47 m 闭环可走。近墙/后排阅读台背后并非全部可通过，主要接近通道在中央与台前。
- **已验证**：Chrome WASD 步进、圆台拦截、侧面通道、后排 E 选书、拖拽环顾/背面空间、Esc 返回；原版开书进入《守月人》、列表搜索与直达、返回、新建取消、成书归位、刷新恢复、减少动态时直接进入。业务测试使用 `localhost:4173` 独立 origin；交付 `127.0.0.1:4173` 仍保留三本示例。生产构建通过，产物 GLB 与源导出逐字节一致，检查控制台错误 0 条。证据在 `evidence/v012/`。
- **资产与限制**：三份 GLB 合计 12,247,740 bytes，房间 110,522 三角形。Book 30 网格/1976 顶点、Hands 14 网格/1700 顶点与 baseline 世界坐标最大差 0；手部动作仍为原版，接触细节未改。三张原创生成纹理已打包；新装饰 atlas 原图 1254×1254，生成来源/哈希/象限 UV 见 `assets/source/hearth-decor-atlas.json`。网络参考图未用作运行资产。
- **性能与交付边界**：原生 DPR 2、画布 DPR 上限 1.5 的多页测试中观察约 39–64 FPS；最终 1512×756 / DPR 1 单页截图显示 120 FPS、加载 1.1 s。两种条件不可直接对比，HUD 时间不是严格冷启动首帧计时。仍为独立 Chrome 样片；无真实 StoryOS 集成、未发布外网。本轮改动保留在 `iteration/next` 工作区，未提交、未推送、未新增标签；`main` 与既有标签未移动。视觉细节后续以用户实际体验为准。

### 历史交接（以下保留此前阶段的范围与状态）

- **2026-09-14 开始实施：用户在确认“在当前版本继续调整、没有额外方向问题”后明确回复“好，请开始吧”。** 已授权从恢复版落实所选第一张的整体视觉；不再受上轮“只修壁炉”的实施范围限制。当前正在制作，尚未完成 Chrome 验收。保持 `iteration/next` 与原有交互，手部动画暂缓；同步模型、书位与碰撞坐标，验证真实绕行空间。后续状态更新覆盖以下历史选型阶段的“本轮不改代码”描述。
- **最新确认（2026-09-14）：用户选择第四轮第一张，并连续确认“太棒了”“就是这个”。** 确定的整体视觉目标为 `design/round-04/01-open-hearth.png`（聊天中第一张实际生成结果，非第三轮网络 T01，非早期概念图 1）。保持这张图的暖炉光、冷夜窗、木材与灰泥质感、开阔地面及整体空间关系。停止扩展新风格；此前助手对中轴/规整感的批评保留为历史分析，不再作为必须改掉所选画面的要求。
- 本次确认属于整体视觉选型，仍需在真实三维房间里验证自由走动、各阅读台接近空间及圆台绕行；手部动画暂缓、五个稳定展示位和 Quick Access 沿用。详见 `design/iterations/03-open-hearth-selected.md`。本轮仅记录选择，没有把概念图宣称为已实现效果；当前运行资产仍为恢复版。
- **2026-09-14：用户已对第三轮参考作分项反馈，继续审美对齐。** 明确喜欢 T01/T02 的光和感觉，但认为其布局太挤；强调房间必须留出自由走动空间。T03 的空间感获认可，配色被否定。此反馈不等于采用任一整图布局，未逐项认可家具、道具或建筑细节。手部动画继续暂缓。
- 三张概念的实际显示顺序为：1 → `design/round-04/01-open-hearth.png`（已选），2 → `design/round-04/02-gentle-attic.png`（未选），3 → `design/round-04/03-window-bay.png`（未选）。每次附上 baseline 截图、T01 和 T03 原图；prompt、来源路径、实际尺寸 1774 × 887、哈希和逐图复核在 `design/round-04/`。三张均可辨五个展示位，地面有视觉留白。此前关于变体保守、台位规整的评价仅是选图前的助手分析，最新用户确认以上方记录为准。
- **本轮验证限于概念输出与保存**：三张 PNG 复制完整性通过，已逐图视觉查看；输出不构成真实尺度、碰撞或漫游验证，没有新增功能测试或运行场景验收。
- 第三轮原图与反馈在 `design/reference-library/round-03/`。S11/S12 的环境基准继续有效；“左二右三”仍未授权改变数量。恢复版 `v0.1.1-preview.1` 仍是当前运行基准；“只修壁炉”记录的是上轮完成的实施范围。本轮不改场景代码、模型或交互，不移动分支/标签，不提交或发布。
- **2026-09-14：用户实际体验后要求退回第一版，只优化壁炉空隙。** v0.2 的生活感布局不再是当前实施目标。最新决定见 `design/iterations/02-baseline-hearth.md`，此前讨论保留为历史。
- 当前预览为 `v0.1.1-preview.1`，分支 `iteration/next`；以新提交恢复 baseline，不重写历史。`v0.2.0-preview.1` 保留；`main` 和 `v0.1.0-baseline` 不移动。公开仓库 `https://github.com/FrankQDWang/StoryOS-blender`。
- **已实现**：前端 `apps/web/src` 与 baseline 完全一致，恢复原房间布局、五个独立阅读台、中央魔法圆台、灯光、镜头及原版手部开书。第二版的书柜、休息角、桌面开书和开发检查页已退出当前预览。
- **唯一场景调整是壁炉**：保持第一版的位置、占地、木台面、蜡烛与木柴；两侧改连续炉身，加完整拱口，连接炉台和上沿，用浅石缝代替贯穿空隙。复用原纹理，不新增外部素材。
- **已验证**：Chrome 全景与书本/壁炉近景，原版完整开书进入《星海余烬》、返回、中央新建表单取消；4 项领域测试、生产构建通过。保存模型的两侧 330 条射线抽查，baseline 16 处漏空，当前 0 处；拱口上沿 25/25 命中，炉膛仍开放。该抽查不是全场景物理碰撞证明。
- 书本/手部源网格全部顶点位置与 baseline 一致（精度 0.01 mm）；恢复原版动作也意味着第二版手部接触改进不在本轮范围，不能沿用 v0.2 接触验收结论。
- 3 份 GLB 合计 6,049,352 bytes；房间 35,668 三角形，书本 3,788，手部 3,224；两张纹理打包进 `.blend`。版本和哈希见 `assets/manifest.json`。
- 当前预览 `http://127.0.0.1:4173/`；浏览器原有三本示例保留。截图、模型检查、测试和构建日志在 `evidence/v011/`；v0.2 证据保留于 `evidence/v02/`。
- 本机 Chrome 开发态本次观察约 41–45 FPS，页面显示加载 1.9 秒；只是当前机器和缓存条件，不是跨设备性能承诺。
- **交付边界**：独立桌面体验样片，真实 StoryOS 未修改，工作区仍为占位。第一版已获认可，修复后的预览供用户继续查看，不自动合入 `main`。
- 制作命令使用 Blender `--factory-startup --python-exit-code 1`，避免本机节点命名设置干扰和 Python 出错后仍索引旧资产；不修改用户 GUI 设置。
- 授权继续有效：Chrome 控制、Blender CLI/GUI 自主选择、必要依赖安装、公开 GitHub 保存；资源留本目录。无新增权限要求。

## 开工基线（2026-09-13）

用户认可当前方向，要求仅确认影响整体方向的事项。以下沿用已接受范围，作为实施基线：

1. **交付对象**：本目录内的独立桌面 Chrome 体验样片；使用示例书籍与本地新建数据，开书进入占位工作区。真实 StoryOS 仍是后续集成阶段。
2. **视觉**：私人、低密度、夜间的魔法藏书室；S11/S12 的整体环境风格，S19 指定角色的造型简化程度。以有雕塑感的立体造型和电影光影为方向；图片中的人物、床铺与屏风不是功能或陈设需求。
3. **主要流程**：五个稳定展示位；可选第一人称漫游与直接快捷入口；预制镜头、手部和翻书转场；中央装置创建新书并成书归位；返回和本地保存。
4. **技术与素材**：Blender 制作可复用真实 3D 资产和动画，网页运行 React/R3F/Three.js；场景自制为主，网络参考图仅用于视觉研究。项目资产和依赖优先保存在本目录，共享程序通过既有包管理器管理。
5. **第一个可评审检查点**：一间有材质与灯光的房间、一本可打开的书，以及进入/返回占位工作区的最短流程。内部几何体和概念图可以用于制作验证，不能替代最终可漫游场景。此检查点不缩减最后的五个展示位、手部开书、快捷入口或魔法新建流程。
6. **决策方式**：家具造型、书封、木色与灯光细节由助手按已选参考自主处理，通过实际画面迭代。只有实质产品取舍、付费素材/外部服务承诺或超出现有授权的资源存放与权限才另行询问。

开工时无需用户补充素材、账号或 API 密钥。现已完成工具安装、资产制作和独立样片验收；最新状态见“当前交接”。

## 需求来源与已确认决定

来源：ChatGPT 对话《Blender入口页方案》，conversationId `6aa25d50-9030-83ee-b8ae-dae502f41746`。

2026-09-11，用户明确纠正：“我希望按照推荐的来，不是我一开始的要求”。本项目以该对话最终推荐的方案为基准。较早的 StoryOS 研究文档只作参考，不覆盖本次决定。

2026-09-13，用户对独立样片建议回复：“可以，没问题”。接受的样片范围：示例项目、完整入口体验、开书后的占位工作区；体验成立后再考虑接入真实 StoryOS。

2026-09-13，用户针对第一轮概念图补充：功能核心均认可，仅调整视觉；要求真实感、私人作家的魔法藏书室、更日常、元素更少。用户不需要使用设计术语描述目标，希望通过网络参考图、逐轮提问与可视化比较发现偏好。此决定覆盖此前“风格化半写实”及强哥特、观星装饰的美术默认值。

随后用户选择真实照片组的第三张（C，Kipling 的 Bateman's 书房），明确喜欢木色和光影，并补充：“不要做完全真实的，做魔法。或者带点卡通的那种”。保留可信空间与材质基础，允许并探索艺术化、轻度卡通；具体程度尚未确认。该“第三张”不是第一轮生成图 3（Moonlit Orrery）。

后续整体否决（覆盖上述照片与生成图偏好）：用户说“我要彻底否决这套审美风格，不喜欢，不要阳光，魔法，奇幻，卡通，应该是这种的，元素不要太写实”。含义为不要阳光，目标是魔法/奇幻/卡通；不是否定魔法。此前写实书房照片与生成图不再作为锚点，功能核心和低密度私人空间仍保留。

较早的第一批图片反馈进一步限定上述用语：“R12 ·《魔弦传说》微缩布景实拍 这个接近，其他的都不是”。因此“元素不要太写实”不能被机械理解为排斥一切真实材质或空间光影，也不能强制常见圆润/鲜艳/厚边卡通。当前探索可感知制作痕迹的立体奇幻布景，但手工纹理、木色、图案和照明仍是待验证分析；R12 仅接近，尚未定稿。

最新的第二批反馈：S11/S12 整体环境风格获认可，S19 仅指定角色画风获认可，屏风与 S05/S14/S18 均被否定。因此不再沿用“手工感、纸艺或大幅图绘就是用户偏好”的广义归因，后续以上述两张环境图及角色局部为具体参照。详见 `design/reference-library/round-02/feedback.md`。

## 产品目标

为小说作者提供一个具有空间感与作品归属感的项目入口。一本书代表一个小说项目。用户可以在私人魔法藏书室中选书，也可以直接继续写作。

样片必须帮助用户判断实际画面、操作与转场是否值得进一步投入。概念图和灰盒分别提供美术、交互证据，均不能单独宣称完成样片。

## 体验与空间

| 项目 | 采用的方案 |
| --- | --- |
| 空间 | 一个小型私人作家的魔法藏书室；亲切尺度、少量主要物件与清楚留白。以 S11/S12 比较环境风格，具体建筑、床铺和树洞不是已选布局 |
| 美术 | 主要环境参考 S11/S12；S19 仅借指定角色画风。朝有雕塑感的童话三维与夜间电影光影收敛，具体分项待画面验证；不要阳光。纸艺、木版画、屏风与 S18 不是正向参考 |
| 作品陈列 | 恢复第一版的 5 个稳定独立阅读台；用户看过第二版后选择退回第一版 |
| 观看与移动 | 第一人称视角，可选择进入漫游；用户无需漫游也能完成主要操作 |
| 快捷入口 | 可发现的继续最近作品、搜索与完整项目列表；不依赖用户先发现快捷键 |
| 书本信息 | 靠近或选中时出现清晰 HTML 信息和打开操作；场景中的标题是动态项目数据 |
| 开书 | 恢复第一版固定镜头、预制手部与翻书转场，进入占位工作区 |
| 新建 | 中央魔法圆台 → 输入书名 → 成书动画 → 放入空阅读台 → 回到全景 |
| 工作区 | 独立普通 HTML 页面，显示所选作品身份并可返回藏书室；仅作入口转场占位 |

首版不增加人物全身、NPC、开放地图、自由抓取、IK、手部物理、多人协作或真实编辑器业务。

## 样片数据与边界

- 使用明确标为演示的项目数据；样片新建项目可本地持久化，刷新后仍存在。
- 初始示例保留可用展示位，以便体验新建流程。
- 展示位数量不是项目数量上限。所有项目始终可从完整列表进入。
- 展示位置保持稳定，最近编辑排序只影响快捷入口。展示位满时，新项目保留在完整列表中并清楚说明，不丢失、不静默挤走原作品。
- 创建取消不生成项目；重复点击不创建重复项目。动画结束不代替创建成功状态。
- 字数、进度、时间等若出现，属于样片数据。正式接入前重新检查真实 API 字段，不将画面示例当成现有接口承诺。
- 不修改、调用或迁移真实 StoryOS 项目数据，不实现真实编辑器、账户、云同步或生产发布。

## 技术边界

- Blender 是资产制作工具，产出可重复使用的场景、书本、动画源文件及 GLB。
- 浏览器以 React、React Three Fiber、Three.js 和必要的辅助库运行实时 3D。
- 场景资产与项目数据分开。新建项目在运行时生成书本实例，无需修改 Blender 文件。
- 使用真实 3D 几何体与相机运行漫游。概念图不是最终可漫游场景的平面背景替代品。
- 静态环境、动态书本、镜头/交互状态、DOM 管理界面与占位工作区各自有清楚责任。
- 软件版本、导出设置与资产来源在采用时记录。需要具体资源时再下载，避免先安装大量未使用工具。

## 默认实施细节

以下是可调整的样片实施默认值，不改变已确认的产品方向：

- 桌面 Chrome 优先；鼠标与 WASD 漫游，Esc 恢复普通指针。通过屏幕按钮也能完成选书、创建与进入。
- 搜索使用可见按钮与 Cmd/Ctrl+K；不占用 Tab 的标准焦点导航。
- 保留降低动态效果和列表入口；动画可跳过，低动态模式使用短淡入淡出。
- 不使用镜头晃动或强制长距离移动。转场目标简短，具体时长以实际体验调整。
- WebGL 不可用、资源加载失败或低性能时，DOM 项目入口仍可用。
- 支持从占位工作区返回并恢复合理的藏书室选择状态。
- 先使用免费或现有许可适合的素材；发生付费购买或第三方服务承诺前，由用户确认具体对象。

## 授权与资源存放

用户已授权：操控 Chrome；下载工作必要的依赖、程序与资源；持久化本方案。

首选位置为本项目目录。共享程序可由 Homebrew、npm 等既有管理器安装；项目依赖采用项目本地安装。

| 内容 | 位置约定 |
| --- | --- |
| 方案与交接 | `PLAN.md`、`AGENTS.md` |
| 视觉参考与生成提示 | `design/` |
| Blender 源文件与制作脚本 | `assets/source/`、`scripts/` |
| 网页使用的资源 | `public/assets/` |
| 未处理下载 | `downloads/` |
| 截图与验证记录 | `evidence/` |
| 应用依赖 | 项目目录内的依赖环境与锁文件 |

内置图片生成工具有固定的 Codex 输出目录；生成后将本项目的交付图片复制到 `design/`，并记录来源。该工具管理的初始路径不当作应用资源路径。

有实质体验取舍、需要其他安装位置或出现权限边界时再询问。明显更优的常规实现决定自主处理。

## 执行顺序与验收

1. **视觉目标**：大方向已获认可。将 S11/S12 环境风格和 S19 指定角色画风转成私人藏书室构图；不要阳光，保留低密度。按需制作局部概念或场景预览，旧第三轮提示不直接执行；不再把泛搜参考或逐项审美问答作为开工前置条件。
2. **交互骨架**：跑通选书、漫游、快捷入口、创建、开书、返回、刷新恢复和降级；简单几何体仅作为内部验证阶段。
3. **美术资产**：制作或整合可复用 3D 环境与动画，保留源文件、脚本和来源；将选定参考落实为可运行场景。
4. **Chrome 验证**：检查真实视口画面与主要交互；记录截图、实际结果和限制。完成必要修正。
5. **样片交付**：提供运行入口、启动方法、资源清单、验收结果与剩余限制；供用户决定后续集成。

完成样片须同时满足：

- 实际浏览器中的场景有完整材质、光照与统一风格，能与选定视觉参考比较。
- 用户可选择漫游，也可直接打开作品；五个展示位与完整列表对应同一数据。
- 至少一本动态书能完成预制开书转场，正确进入对应的占位工作区。
- 魔法台新建成功后成书并放置，取消与容量已满的情况可理解，刷新可恢复。
- 返回藏书室、键盘操作、降低动态效果和 3D 失败降级可用。
- 报告实测的加载与帧率情况；不将“60 FPS”或“几 MB”预期写成已验证事实。
- 可通过记录的依赖、脚本和源资产重新运行与导出。

## 实际进度

下表保留调研历史；当前实现状态以文首交接为准。

| 日期 | 结果 |
| --- | --- |
| 2026-09-13 | 确认目录为空；Node、npm、pnpm、Homebrew、Python 与 Chrome 已存在；PATH 与 `/Applications/Blender.app` 未发现 Blender，尚未安装。 |
| 2026-09-13 | 创建方案与接手说明；尚未启动应用构建或浏览器验证。 |
| 2026-09-13 | 内置 ImageGen 生成并展示三张独立概念图，读取 PNG 文件头确认尺寸均为 1487 × 1058。全部已复制至项目目录，PNG 格式与复制完整性检查通过。用户尚未选择，应用未构建。 |
| 2026-09-13 | 用户否定第一轮视觉方向的密度与公共仪式空间感，保留功能核心；转入真实私人书房参考调研。参考、来源与待确认问题记录于 `design/style-discovery.md`。 |
| 2026-09-13 | 用户认可真实照片 C 的木色与光影，要求魔法/轻卡通。内置 ImageGen 生成第二轮三张参考，全部复制到 `design/round-02/`，源文件一致性、PNG 格式与提示映射检查通过。尚未选定。 |
| 2026-09-13 | 用户全面否决前两轮审美，明确要求无阳光、魔法、奇幻、卡通。随后要求扩大跨媒介搜图；暂停生成，建立 12 张主要参考的本地图集，来源与边界均已持久化，等待图片偏好反馈。 |
| 2026-09-13 | 完成 Blender 原生资产、React 3D 样片、完整本地项目交互、Chrome 验收；资产与截图均在项目内，待用户体验与视觉反馈。 |

## 视觉参考交接

编号以下列三张实际生成图的聊天展示顺序为准，不包括生成前读取的编辑器参考图。

| 展示编号 | 本地文件 | 状态 |
| --- | --- | --- |
| 1 | `design/concepts/01-rose-window-scriptorium.png` | 历史方案；未采纳，作为过密/过仪式化的反例 |
| 2 | `design/concepts/02-hearthside-cabinet.png` | 历史方案；未采纳，作为过密/过仪式化的反例 |
| 3 | `design/concepts/03-moonlit-orrery.png` | 历史方案；未采纳，作为过密/过仪式化的反例 |

当前均为视觉概念，不是浏览器截图或运行效果。生成图的细节不覆盖产品规则：首版保持 5 个展示位，初始项目留有新建空位；标题在实现中来自清晰的动态 DOM 文本。第一张生成图显示了 4 本书，实施时根据样片数据调整占用数量。装饰性文字与书封细节不是新增业务需求。

本轮三张保留用于追溯，不作为实施目标。后续每轮使用独立编号，避免将网络参考图与旧生成图混淆。用户确认修订视觉后再更新所选项。
