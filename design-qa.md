# StoryOS 当前房间视觉验收

日期：2026-09-16。资产版本：0.1.2-preview.4。用户实际体验后确认“可以，这个我比较满意”，并要求提交保存、push。当前 0° / 10° / 20° 角度方案已获认可；上一轮 preview.3 已被用户否定。

## 后排摆正与角度渐变验收（本轮）

- 后排两台 yaw 0°，右中 -10°，左右前景 +20° / -20°；五台位置及阅读斜面沿用。台面/支架、书本、镜头和碰撞继续读取统一布局。
- 证据 `evidence/v012-gentle-angles/`：`01-before.jpg` / `02-after.jpg` 为同视口 1512×751 前后实际截图，后排前沿摆正，侧边与前景的转角收小；五个展示位、壁炉可见性和中央留白保持。`07-overview-final.jpg` 是交付全景。
- Chrome 后排点击、近景切漫游、W 接近后 E 选书、完整开书进入《守月人》并返回、前景选中与 20° 近景均通过；对应截图 `03-rear-focus.jpg` 至 `06-front-focus.jpg`。交付仍为 3 本示例，未新增测试数据。
- 10 项既有测试、Blender 实际几何与生产构建通过。五台站位、聚焦和开书落点可走，镜头安全修正无需触发；圆台闭环通过。Book/Hands 源顶点与 baseline 一致、生产 GLB 与导出一致、Chrome 错误日志为空。保留原手部接触细节及指针锁定降级限制。
- 技术检查先完成；随后用户在实际体验后明确认可当前效果。此次认可不扩大为手部细节、其他设备或正式集成验收。

## 后排朝向衔接验收（0.1.2-preview.3，效果已被用户否定）

- 后左/后右改为 +0.18/-0.18 rad，各内收约 10.3°；其余角度与五台位置保持。后排仍基本迎向入口，右侧由后到前的内收逐渐增强。
- 本轮证据在 `evidence/v012-rear-inward/`。`01-before.jpg` / `02-after.jpg` 均为 1512×751，同入口视角、3 本示例；后排外撇消除，五台与中央空地、壁炉可见性保持。`07-overview-final.jpg` 为最终 1512×695 画面；此尺寸不用于与前两张直接测量比较。
- `03-left-focus.jpg`、`04-left-roam.jpg`、`05-right-focus.jpg`、`06-right-opening.jpg`：后排两书直接选中，左后原地切漫游、W 接近后 E 选书，右后正常开书进入正确工作区并返回均通过，书与台面/镜头方向一致。
- 修正 P2 点击误拦截：运行时确认中央台 Sparkles 的 Points 首先命中，距点击射线约 0.97 m，点击后排书却打开新建。仅对该装饰 Points 禁用 raycast；原坐标复测选择《守月人》，右后也可直接选择；圆台实体创建入口及取消仍有效。诊断见 `particle-picking.json`，没有修改全局事件坐标或粒子外观。
- 10 项既有测试通过，五台站位/聚焦/开书投影可走，圆台闭环通过；保存后的 Blender 几何与统一布局一致，Book/Hands 顶点保持；最终构建通过、GLB 产物与导出一致、Chrome 错误日志为空。本轮无阻止预览的 P0/P1/P2，原手部接触等后续细节继续暂缓。

## 上一轮局部转向验收（0.1.2-preview.2）

- 用户授权保持房间构图和五台位置，左前/右中/右前适度转向中央通道；后排两个保留迎入口关系。实际 yaw 为 +0.48 / -0.36 / -0.52 rad。支架与书台整体同步，既有灯光、材质、手部动作保持。
- Chrome 同视口前后：`evidence/v012-orientation/01-before.jpg`、`02-after.jpg`，均为 1512×751 截图。前景书封更正面、右侧台面向中央收拢，炉口没有被重新遮住，中央留白保持。其余测试页截图为 1512×695，未用来做尺寸前后比较。
- 左前/右前聚焦与开书：`03-left-focus.jpg` 至 `07-right-opening.jpg`。台面、书、手部组和镜头方向一致，完整动画进入对应工作区。独立 localhost origin 补齐第五本测试书，确认新书按新 yaw 归位；交付 origin 仍为 3 本示例。
- 修正阅读镜头转向后右后台落点进入邻台碰撞缓冲区的问题：从台前可达点调用实际移动碰撞函数，取得最近可站位置。`08-rear-focus.jpg`、`09-rear-roam.jpg` 验证近景原地切漫游，`10-walk-approach.jpg` 及随后 E 选书通过。所有五台聚焦/开书终点水平投影可走，见 `camera-clearance.json`。
- 既有 10 项测试通过；Blender 保存网格与布局、碰撞一致，Book/Hands 世界顶点与 baseline 最大差为 0；最终构建通过、产物与源 GLB 一致；两页 Chrome 错误日志均为空。证据为本目录的 `tests.log`、`geometry-check.json`、`build.log`、`asset-parity.json`、`console-errors.json`。
- 本次局部验收无 P0/P1/P2。此前手部接触/素材精度等限制继续保留，未扩大为整体美术重做或正式 StoryOS 集成。

以下保留 2026-09-14 / 0.1.2-preview.1 的整体氛围验收记录；对应旧证据不代表本轮重新测试了所有历史流程。

## 对照证据

- source visual truth：`design/round-04/01-open-hearth.png`，1774×887，已选概念。
- implementation screenshot：`evidence/v012/overview.png`，Chrome，1512×756 CSS / 截图像素，deviceScaleFactor 1。
- state：全景、夜间、3 本示例 + 2 个空位、正常动态、原有 DOM 入口。
- full-view comparison：`evidence/v012/comparison-final.png`；通过本地 `compare-final.html` 将原图和实际截图放入同一画面，双方等宽且保持 2:1 比例，没有拼画或修改场景截图。
- focused comparison：同张对照下排放大炉口、炉台、木梁、灰泥及后窗。正常开书近景另见 `opening-book.png`；新建和漫游另有截图。
- 首轮截图 1512×751；中间原生窗口变为 1512×695。中间尺寸变化不用于精确比例结论；最终用 CDP 校准 1512×756 / DPR 1 后重新截图，交付前恢复临时尺寸设置。
- 概念没有 UI，实际保留基线的顶部导航、左侧标题与底部 Quick Access；这部分不是遗漏还原。真实三维资产和运行时光照取代概念的离线画面，不能宣称逐像素一致。

## 比较历史与修正

1. 首轮 `overview-pass-1.png` / `comparison-pass-1.png`：P1 左前书挡住炉口；P2 烟囱过暗、地板光池窄、材质在全景偏平、窗外树形过于简单。
   - 将前景书台向外侧调整，中心通道保持；模型、书位与碰撞同步。
   - 增加炉台局部烛光、降低地板粗糙度；重新铺设米制 UV，增加木纹色差与灰泥表面法线变化。
   - 方形弯曲梁、连续烟囱表层及薄石缝补充实体形状；夜窗采用蓝色层次及多层带分枝的真实树影。
2. 中间 `overview-pass-2.png`：前景遮挡改善，但有视口变化，不能将比例差异作为回归。近景火舌有水滴感，按 P2 修正为底部展开、顶部弯曲收窄的体积火焰，并提高高度。
3. 最终 `overview.png` / `comparison-final.png`：全景与局部重新同框比较。炉口保持可见、暖光在炉台和地板连续分布；木/灰泥有表面读感；五台和中央留白成立。上述 P1/P2 已处理，当前无阻止本轮评审的 P0/P1/P2。

## 五项必查表面

| 表面 | 实际检查与结论 |
| --- | --- |
| 字体与排版 | 概念无应用文字。保留原版 Cormorant 品牌/既有中文字体与层级；Chrome 中标题、按钮、表单、作品列表和工作区可读，没有新截断或重叠。 |
| 空间与节奏 | 五个独立阅读台、左炉、后窗书桌、中央圆台保留。前景台外移以露出炉口，未增加展示数；五台正面可达，中心闭环通过，近墙台后侧不是全部可走。 |
| 颜色与光照 | 暖琥珀局部光、棕木/灰泥、冷蓝夜窗和低亮绿色魔法环符合所选组合，没有引入被拒绝的紫粉主色。桌灯比首版收敛，火炉地板反光成为重要视觉线索。 |
| 图像与材质质量 | 木纹/灰泥及新装饰图集有真实纹理 UV；没有用概念背景替代房间。图集 1254×1254、每格627像素，常规距离清楚、无象限串色；近到墙面可见有限分辨率。图案来自 ImageGen 原图，画框/家具为真实几何。 |
| 内容与文案 | 保留原业务文案、示例名称和5位规则；场景里没有把设计说明写成产品文字。创建取消不增作品，测试新书归位且列表与工作区身份一致。 |

## 行为与工程验证

- Chrome：WASD 多次步进到圆台前、沿侧面到后排，E 选择《守月人》，原版开书完成进入对应工作区；拖拽环顾到背墙/门，Esc 返回全景。
- 指针锁定在自动化会话未获得，实际走通的是既有拖拽环顾降级路径；本轮未把原生锁定成功写成已测结果。
- 创建取消、中央入口新建、成书归位、刷新保存、Cmd+K 搜索、列表直达、返回、减少动态直接进入均通过。新建用 localhost origin，交付 127.0.0.1 数据仍3本。
- `npm test` 10/10；真实移动函数覆盖五台正面路线和圆台闭环。`scripts/check_scene.py` 检查保存后的实际网格，五台与旋转碰撞一致，桌椅突出部被容纳，Book/Hands 顶点与baseline无差异，壁炉柱/拱口射线通过。
- `npm run build` 通过，生产GLB与源导出逐字节一致；`console-errors.json` 为 `[]`。上游 THREE.Clock 提醒和 Vite 大分块提醒保留，不影响本轮已测行为。
- 实际截图时1512×756、DPR1显示120 FPS/加载1.1s；原生DPR2多页场景中观察39–64 FPS。不同条件不能直接比较；没有跨设备或严格冷启动承诺。

## 后续细节与边界

- P3：窗外细枝、植物枝叶及火焰形态仍比概念更概括；灯光没有离线全局照明的全部柔和反射。保留为后续美术细化点，不把当前版本说成概念图级最终渲染。
- P3：极近距离挂毯可见627像素纹理的清晰度上限，后续有更近的固定镜头时再制作高分辨率独立纹理。
- 按用户要求，手部造型/接触与预制动作本轮暂缓；工作区仍为占位，真实 StoryOS 未集成。移动端、其他浏览器、低端显卡未重新验收。

## 实施清单

- [x] 落实所选方向并同框比较全景和局部。
- [x] 修正遮挡、材质读感、暖光分布及火焰形态。
- [x] 同步三维几何、书位与碰撞，完成实际流程及必要测试。
- [x] 保留概念、源文件、资产来源和 Chrome 证据。

technical result: passed; aesthetic review: user accepted 0.1.2-preview.4 on 2026-09-16
