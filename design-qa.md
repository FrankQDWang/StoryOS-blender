# 私人藏书室样片验收 · 2026-09-13

final result: passed

通过范围为已授权的独立桌面体验样片。此结论不代表用户已经认可本轮实际美术，也不声称达到电影制作精度。

## 比较目标与证据

- 主要视觉目标：`design/reference-library/round-02/images/S11.webp`（2500 × 1382）和 `S12.webp`（2500 × 1376）。S19 仅作指定角色造型参考；未把人物、床铺、屏风、树洞原样搬入产品。
- 实现：`http://127.0.0.1:4173/`；最终全景 `evidence/library-overview-final.png`，1512 × 695 CSS px / 截图像素，1×。初始 3 本作品、全景、普通动态、深色主题。
- 全景比较：在同一次图像输入中打开 S11/S12 与 `evidence/library-overview.png`；修正灯光后，再同次打开 S12、`evidence/library-overview-final.png`、`evidence/library-focus.png` 和 `evidence/qa-create-form.png`。不是仅依据路径或文字判断。
- 原片属于环境风格参照而非 UI 稿，构图和物件不同，不做逐像素复刻评分。显示工具将原片按比例缩至宽 2048；网页截图保持 1×；没有把画幅比例差异误算成布局错误。
- 重点区域：`evidence/library-focus.png` / `library-opening.png` 展示书封、纸页、手和可读信息卡；`qa-create-form.png` 展示 1100 × 750 下的表单、颜色与焦点环。近景足够直接观察，无需再次裁切原片。

## 五项视觉检查

| 项目 | 观察与结论 |
| --- | --- |
| 字体 | 原片无产品 UI 字体可复刻。拉丁品牌使用本地 Cormorant Garamond，中文标题使用 macOS 宋体回退，正文系统无衬线。标题/说明/操作有层次，近景卡与表单文字可读。未宣称跨平台字形完全一致。 |
| 间距与布局 | 主画面留出顶部工具、左侧说明、底部直接入口。5 个单独阅读台，不铺满书架。1512 × 695 和 1100 × 750 下主要操作完整可用；较窄画幅会裁切边缘 3D 家具，列表仍提供完整作品。长标题增加省略/换行约束，避免挤走常驻操作。 |
| 色彩 | 暖木/灰泥、夜窗冷光、烛光局部暖色、魔法台克制的绿色光。修正全房间泛亮后，书桌/烛火与暗处有更清楚区分；无阳光。比原片更清晰的环境亮度用于交互识别，属于样片的有意取舍。 |
| 资产质量 | 场景、书本、手为 Blender 真实几何，支持真实透视和移动；两张原创纹理贴在几何上。没有使用网页 CSS/SVG 代替参考环境或平面背景冒充 3D。家具和手部的简化程度仍明显高于电影，保留为后续美术细化项。 |
| 文案与图标 | 中文操作、作品身份、字数、本地样片标识及容量说明一致。项目名称来自数据。通用图标统一使用 Phosphor；未复制电影标志/水印。工作区明确说明占位性质。 |

## 修正历史

| 原问题 | 级别 | 修正和复验 |
| --- | --- | --- |
| 未漫游时点书误触中央魔法台 | P0 | 移除未锁定时仍改写射线的控制器；Chrome 点击左侧实体书准确显示「星海余烬」，见 `library-focus.png`。 |
| 浏览器拒绝指针锁定后无法漫游 | P1 | 原生指针锁定 + 拒绝时的拖拽方式；提示同步变化。WASD 小步前进、拖拽转向和 Esc 返回均在 Chrome 操作，见 `qa-roam-start.png` / `qa-roam-look.png`。 |
| 选中悬浮标题挡书、封面铰链与阅读台斜度不合 | P2 | 只在未选中的悬停状态显示标题；保留世界变换设置铰链，调整书倾角。近景和打开状态见 `library-focus.png` / `library-opening.png`，信息卡未挡住选中书。 |
| 整体补光过平、削弱夜间冷暖关系 | P2 | 降低环境与侧墙补光，提高书桌局部光，保留窗边冷光；同视口由 `library-overview.png` 复拍为 `library-overview-final.png`，与 S12 再次同时比较。 |

最终比较没有需阻止**首版体验评审**的 P0/P1/P2；未将已知美术精细度差距隐藏为“完全还原”。

## 浏览器交互结果

| 流程 | 实测 |
| --- | --- |
| 全景 → 点书 → 聚焦 → 预制开书 → 工作区 → 返回 | 通过；对应标题正确，封面、纸页、两只手可见，跳过按钮存在。 |
| 快捷最近作品、列表、搜索 | 通过；搜索「守月」只返回对应作品，直接进入正确工作区。 |
| 创建取消 / 空输入 | 通过；空书名不能提交，输入后 Esc 取消不会增书。 |
| 魔法创建与本地保存 | 通过；第 4 本成书归位，第 5 本用最后空位，第 6 本提示列表保存；刷新后 6 本及低动态设置保留。创建状态在动画开始前落入本地数据。 |
| 展示位稳定与容量 | 通过；先前三本仍在 1/2/3，第 4/5 本在 4/5，第 6 本仅在列表且可进入。见 `qa-project-list.png` / `qa-workspace.png`。 |
| 降低动态 | 通过；开启后创建立即完成，设置刷新保持。开书绕过长转场的分支已实现；未为所有书逐一重跑低动态。 |
| 漫游 | WASD 小步位移、拖拽环顾、Esc 恢复全景通过；前台 Chrome 曾接受指针锁定。未进行所有障碍物角落的长时间碰撞巡检。 |
| 失败降级 | 故意拦截 room GLB 后提示失败，快捷进入仍到正确工作区。见 `qa-fallback.png`；此截图早于隐藏失败状态魔法提示的小修正。网络拦截已恢复。未另行关闭整个 WebGL 子系统。 |
| 键盘 / 较小窗口 | 表单 Tab 焦点可见、Esc 关闭；1100 × 750 主控件与创建表单可用。见 `qa-desktop-1100.png` / `qa-create-form.png`。 |

验收数据使用 4174 的独立 localStorage；主预览 4173 保留三本示例、两个空位。验收标签/服务已关闭，视口和网络拦截已恢复。

## 工程和性能证据

- `evidence/tests.log`：4/4 领域测试通过，覆盖超额保存、创建原子性、持久化清洗、访问不移动展示位。
- `evidence/build.log`：生产构建成功。大分块提醒仍在：主 JS 约 1.335 MB / gzip 373 KB。
- `evidence/blender-source-check.log`：源文件可以后台打开，两张纹理均已 packed，Room/Book/Hands 与 4 个铰链存在。
- `assets/manifest.json`：3 份 GLB 共 6,028,460 bytes；校验 GLB 2 容器长度、图片/缓冲内嵌并记录 SHA256。
- 控制台：最终正常预览未见新的应用错误；仍有 R3F/Three 依赖链的 `THREE.Clock` 弃用提醒。故意阻断资产产生的历史错误不当作最终正常流程错误。
- 普通开发态加载采样约 1.1–2.1 秒，画面右下统计约 50–109 FPS；不是清空缓存后的冷启动，也不是最低帧率或跨设备基准。模态框/后台标签暂停连续渲染，返回工作区卸载场景。

## 后续细化（P3）与测试边界

- 增加少量有明确造型目的的家具雕刻，改进木纹铺设与石块轮廓变化，避免靠堆砌陈设增加复杂度。
- 手部接触书页仍较概括；纸页当前为刚性铰链展开，可进一步做弯曲和错峰节奏。没有 IK 或物理手。
- 灯笼亮部、书封金属边仍可继续调；不将此原型当作电影资产精度。
- 环境音仅为可开关的轻声合成和弦，未做听感验收。
- 移动端、其他浏览器、低端显卡、无存储权限/坏数据的全部浏览器组合未逐一测试；前两者不在本次桌面 Chrome 范围。

## 交付检查

- [x] 实际运行的 3D、完整入口核心交互与本地数据。
- [x] Blender 源文件、脚本、嵌入纹理 GLB 和来源记录。
- [x] 浏览器画面、主要交互与构建/领域测试证据。
- [x] 方案更新并明确首版精度、占位工作区和后续集成边界。
- [x] 本地预览启动且 Chrome 体验标签保留。
